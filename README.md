# Автоматический сменщик emoji-статуса в Telegram

Этот проект автоматически меняет эмодзи-статус в твоём профиле Telegram (требуется Premium) в зависимости от времени суток и событий:

- утро (6:00–11:59) → твой утренний эмодзи  
- день (12:00–17:59) → дневной  
- вечер (18:00–21:59) → вечерний  
- ночь (22:00–05:59) → ночной  
- завтрак (7:00–7:10) → breakfast  
- в пути (7:10–8:00 и 17:00–18:00) → transit  
- выходные → weekend  

Скрипт работает на бесплатном сервере Railway и сам проверяет время каждые 10 минут.

## Как запустить у себя (если получил репозиторий)

1. **Склонируй репозиторий к себе**  
   - Зайди на GitHub → нажми зелёную кнопку **Fork** (в правом верхнем углу страницы репозитория)  
   - Теперь у тебя будет своя копия репозитория (например github.com/твой-логин/tg-emoji-status-bot)

2. **Получи свои API-ключи от Telegram**  
   - Открой https://my.telegram.org  
   - Введи номер телефона → получи код в Telegram → войди  
   - Нажми **API development tools** → **Create new application**  
   - Заполни:  
     - App title: любое (например My Emoji Bot)  
     - Short name: любое английскими буквами (например emojibot)  
     - URL: оставь пустым  
     - Platform: Android или Desktop  
     - Description: оставь пустым  
   - Нажми Create application  
   - Скопируй:  
     - **App api_id** (число)  
     - **App api_hash** (длинная строка)

3. **Получи SESSION_STRING (одноразовая авторизация)**  
   - Зайди на https://replit.com → зарегистрируйся (можно через GitHub)  
   - Нажми **+ Create** → **Python**  
   - Вставь в main.py этот временный код (замени api_id и api_hash на свои):

     ```python
     from telethon.sync import TelegramClient
     from telethon.sessions import StringSession

     api_id = ТВОЙ_API_ID
     api_hash = 'ТВОЙ_API_HASH'

     client = TelegramClient(StringSession(), api_id, api_hash)
     client.start()
     print(client.session.save())
     ```
     
   - Нажми Run
   - Введи номер телефона и код из Telegram
   - В консоли появится очень длинная строка — это SESSION_STRING
   - Скопируй её полностью

4. **Разверни проект на Railway**
   - Зайди на https://railway.app → войди через GitHub
   - Нажми New Project → Deploy from GitHub repo
   - Выбери свой форкнутый репозиторий
   - Дождись сборки (1–3 минуты)
   - Зайди в проект → вкладка Variables → Add Variable
   - Добавь три переменные:
       API_ID = твой api_id (число)
       API_HASH = твой api_hash (строка)
       SESSION_STRING = длинная строка из Replit (выбери тип Encrypted)
   - Нажми Redeploy (вкладка Deployments)

5. **Проверь, что всё работает**
   - Зайди в проект Railway → вкладка Deployments → Logs
   - Через 1–2 минуты увидишь:
   - Telethon клиент успешно подключён и авторизованСтатус изменён на ...
   - Открой Telegram → свой профиль → статус должен поменяться (сразу или в течение 10 минут)

## Как поменять эмодзи или время

   - Зайди в свой репозиторий на GitHub → файл main.py
   - Найди словарь emoji_map — там document_id для каждого состояния
   - Чтобы поменять эмодзи:
       В Telegram поставь нужный эмодзи в статус
       Открой web.telegram.org → правой кнопкой на эмодзи → Inspect
       Найди data-doc-id="..." → скопируй число
       Вставь это число в emoji_map вместо старого
   - Чтобы поменять время или расписание — измени цифры в функции periodic_update()
   - Сохрани изменения → commit → Railway автоматически обновится

## Если что-то не работает

   - Проверь логи в Railway — там будет написано, в чём проблема
   - Самые частые ошибки:
       "Сессия не авторизована" → заново получи SESSION_STRING
       "Custom emoji не найден" → смени document_id на рабочий
       Нет изменений → подожди 10–20 минут или redeploy проект
   
   Готово! Теперь статус меняется сам. Удачи! 🌟
