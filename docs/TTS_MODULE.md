# 🎤 TTS (Text-to-Speech) Модуль Karina AI

## 📋 Обзор

Модуль **голосовых ответов** Карины — конвертирует текстовые ответы в голосовые сообщения с помощью нейросетевого синтеза речи (Silero TTS).

### 🔑 Особенность

**Карина отвечает голосом ТОЛЬКО на голосовые сообщения.**

- ✅ Голосовое сообщение → Голосовой ответ
- ❌ Текстовое сообщение → Текстовый ответ (всегда)

Это делает общение более естественным:
- Ты пишешь текстом → Карина отвечает текстом (быстро)
- Ты говоришь голосом → Карина отвечает голосом (персонально)

---

## ✨ Возможности

### 🎭 Женские голоса
- **Ксения** — тёплый, дружелюбный (основной) ⭐
- **Елена** — нейтральный, спокойный
- **Ирина** — выразительный, эмоциональный
- **Наталья** — мягкий, заботливый

### ⚙️ Настройки
- Включение/выключение TTS
- Выбор голоса
- Авто-ответы голосом на голосовые сообщения
- Индивидуальные настройки для каждого пользователя

### 🚀 Производительность
- **Генерация:** 1-2 секунды на фразу
- **Формат:** OGG Vorbis (совместим с Telegram)
- **Качество:** 48000 Hz, моно

---

## 📱 Команды

### `/tts` — Управление TTS

**Использование:**
```
/tts              → Показать настройки
/tts on           → Включить голосовые ответы
/tts off          → Выключить голосовые ответы
/tts test         → Тестовое сообщение
```

**Пример:**
```
→ /tts

Карина:
🎤 Голосовые ответы Карины

Статус: ❌ Выключено
Голос: Ксения (тёплый, дружелюбный)

Управление:
/tts on — Включить голосовые ответы
/tts off — Выключить голосовые ответы
/ttsvoice — Выбрать голос
/ttstest — Тестовое сообщение
```

---

### `/ttsvoice` — Выбор голоса

**Использование:**
```
/ttsvoice              → Показать список голосов
/ttsvoice ksenia       → Выбрать Ксению
/ttsvoice irina        → Выбрать Ирину
```

**Пример:**
```
→ /ttsvoice

Карина:
🎭 Доступные голоса:

⭐ **Ксения** (`/ttsvoice ksenia`)
   Тёплый, дружелюбный. Основной голос Карины.

   **Елена** (`/ttsvoice elen`)
   Нейтральный, спокойный. Деловой стиль.

   **Ирина** (`/ttsvoice irina`)
   Выразительный, эмоциональный. Для важных сообщений.

   **Наталья** (`/ttsvoice natasha`)
   Мягкий, заботливый. Для напоминаний.
```

---

### `/ttstest` — Тестирование

**Использование:**
```
/ttstest
```

**Пример:**
```
→ /ttstest

Карина:
🎤 Генерирую тестовое сообщение...

[Голосовое сообщение]
"Привет! Я Карина, твой персональный помощник."

✅ Тест успешен!
```

---

### `/ttsstats` — Статистика (админ)

**Использование:**
```
/ttsstats
```

**Пример:**
```
→ /ttsstats

Карина:
📊 Статистика TTS

👥 Пользователи:
• Всего: 5
• С включенным TTS: 3

🎭 Голоса:
• Ксения: 2
• Ирина: 1
```

---

## 🗄️ База данных

### Таблица `tts_settings`

```sql
CREATE TABLE tts_settings (
    user_id BIGINT PRIMARY KEY,
    enabled BOOLEAN DEFAULT false,
    voice TEXT DEFAULT 'ksenia',
    volume NUMERIC(3,2) DEFAULT 1.0,
    speed NUMERIC(3,2) DEFAULT 1.0,
    auto_voice_for_voice BOOLEAN DEFAULT true,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Поля

| Поле | Тип | Описание |
|------|-----|----------|
| `user_id` | BIGINT | ID пользователя Telegram |
| `enabled` | BOOLEAN | Включён ли TTS |
| `voice` | TEXT | Выбранный голос (ksenia, elen, irina, natasha) |
| `volume` | NUMERIC | Громкость (0.0-2.0) |
| `speed` | NUMERIC | Скорость речи (0.5-2.0) |
| `auto_voice_for_voice` | BOOLEAN | Отвечать голосом на голосовые |

---

## 🔧 Как это работает

### Архитектура

```
┌─────────────────────────────────────────────────────────────┐
│                    ПОЛЬЗОВАТЕЛЬ                             │
│  Отправляет сообщение → Карина отвечает голосом             │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  1. Проверка настроек TTS                                   │
│     SELECT enabled, voice FROM tts_settings                 │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  2. Генерация ответа AI                                     │
│     ask_karina(text) → "Привет! Как дела?"                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  3. TTS синтез (Silero)                                     │
│     text_to_speech("Привет! Как дела?", voice="ksenia")     │
│     → OGG аудио (128 kbps, 48000 Hz)                        │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│  4. Отправка в Telegram                                     │
│     send_voice(chat_id, audio_data)                         │
└─────────────────────────────────────────────────────────────┘
```

### Код интеграции

```python
# В chat_handler
from brains.tts import get_tts_settings, text_to_speech

# Проверяем настройки
tts_settings = await get_tts_settings(event.chat_id)

if tts_settings.get("enabled", False):
    # TTS включён — генерируем голос
    voice = tts_settings.get("voice", "ksenia")
    audio_data = await text_to_speech(response, voice=voice)
    
    # Отправляем голосовое
    await client.send_voice(event.chat_id, audio_data)
else:
    # TTS выключен — отправляем текстом
    await event.reply(response)
```

---

## 🎛 Настройки

### Голоса по умолчанию

```python
# brains/tts.py
DEFAULT_VOICE = "ksenia"  # Основной голос
SAMPLE_RATE = 48000       # Частота дискретизации
MAX_TEXT_LENGTH = 500     # Максимум символов
```

### Изменение настроек

```python
# Включить TTS
await set_tts_enabled(user_id=123456789, enabled=True, voice="ksenia")

# Сменить голос
await set_tts_voice(user_id=123456789, voice="irina")

# Получить настройки
settings = await get_tts_settings(user_id=123456789)
# {"enabled": True, "voice": "ksenia", ...}
```

---

## 📊 Производительность

### Требования

| Ресурс | Значение |
|--------|----------|
| **RAM** | ~500МБ (модель) |
| **CPU** | ~30-50% (короткие фразы) |
| **Диск** | ~2GB (torch + модель) |
| **Генерация** | 1-2 сек на фразу |

### Формат аудио

```
Codec: OGG Vorbis
Sample Rate: 48000 Hz
Channels: 1 (Mono)
Bitrate: ~128 kbps
Size: ~20-50 KB на фразу
```

---

## 🛠 Установка

### 1. Зависимости

```bash
pip install silero-tts torch torchaudio
```

### 2. Системные требования

```bash
# Ubuntu/Debian
sudo apt-get install -y ffmpeg

# CentOS/RHEL
sudo yum install -y ffmpeg
```

### 3. Миграция БД

```sql
-- Выполнить в Supabase SQL Editor
-- Из файла docs/init_tts.sql
```

---

## 🧪 Тестирование

### Быстрый тест

```bash
# Запустить тест из модуля
cd ~/tg-emoji-status-bot
source karina/bin/activate
python -c "from brains.tts import test_tts; import asyncio; asyncio.run(test_tts())"
```

### Тест через бота

```
→ /tts on
→ /ttstest

[Должно прийти голосовое сообщение]
```

---

## 🔍 Troubleshooting

### "Silero TTS не установлен"

**Решение:**
```bash
pip install silero-tts torch torchaudio
```

### "FFmpeg не найден"

**Решение:**
```bash
sudo apt-get install ffmpeg
```

### "Слишком долгая генерация"

**Причина:** CPU не справляется

**Решение:**
- Уменьшить `MAX_TEXT_LENGTH`
- Использовать более короткие фразы
- Рассмотреть GPU ускорение

### "Голос не меняется"

**Причина:** Модель уже загружена

**Решение:**
```python
# Перезагрузить модель
from brains.tts import tts_engine
tts_engine._model = None  # Сбросить кэш
await set_tts_voice(user_id, "irina")
```

---

## 📚 API модуля

### Основные функции

| Функция | Описание |
|---------|----------|
| `text_to_speech(text, voice)` | Конвертация текста в аудио |
| `get_tts_settings(user_id)` | Получить настройки пользователя |
| `set_tts_enabled(user_id, enabled)` | Включить/выключить TTS |
| `set_tts_voice(user_id, voice)` | Сменить голос |
| `get_tts_stats()` | Статистика использования |
| `get_available_voices()` | Список голосов |

---

## 🎯 Сценарии использования

### Сценарий 1: Голосовое → Голосовое ✅

```
Пользователь: [Голосовое] "Какая погода?"
Карина: [STT] → AI → [TTS] → [Голосовое] "Сейчас +15°C, солнечно ☀️"
```

### Сценарий 2: Текст → Текст ✅

```
Пользователь: "Какая погода?"
Карина: [Текст] "Сейчас +15°C, солнечно ☀️"
```

### Сценарий 3: Смешанный

```
Пользователь: [Голосовое] "Расскажи анекдот"
Карина: [Голосовое] "Колобок повесился 🪢"

Пользователь: "Ещё один!"
Карина: [Текст] "Приходит колобок к леснику..."
```

### Сценарий 4: Включение TTS

```
Пользователь: "/tts on"
Пользователь: [Голосовое] "Как дела?"
Карина: [Голосовое] "Отлично! Спасибо что спросил! 😊"
```

---

## 📈 Метрики

### Полезные SQL запросы

```sql
-- Посчитать пользователей с TTS
SELECT COUNT(*) FROM tts_settings WHERE enabled = true;

-- Популярные голоса
SELECT voice, COUNT(*) as count
FROM tts_settings
WHERE enabled = true
GROUP BY voice ORDER BY count DESC;

-- Активность по дням
SELECT DATE(created_at) as date, COUNT(*) as new_users
FROM tts_settings
GROUP BY date ORDER BY date DESC;
```

---

## 🔒 Безопасность

### Ограничения

```python
MAX_TEXT_LENGTH = 500  # Защита от длинных текстов
TTS_TIMEOUT = 30       # Таймаут генерации
```

### Конфиденциальность

- Аудио генерируется локально (не отправляется в облако)
- Настройки хранятся в БД пользователя
- Голосовые файлы не сохраняются (только в RAM)

---

## 📝 Файлы модуля

| Файл | Описание |
|------|----------|
| `brains/tts.py` | Ядро TTS модуля |
| `docs/init_tts.sql` | Миграция БД |
| `skills/__init__.py` | Команды бота |
| `main.py` | Список команд |

---

**Последнее обновление:** Февраль 2026
