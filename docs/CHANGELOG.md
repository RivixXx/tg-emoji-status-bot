# 🔄 Changelog — Karina AI

## v3.0.0 — Февраль 2026

### ✨ Новые возможности

#### 🔌 Система плагинов
- **Модульная архитектура** — подключаемые модули для расширения функционала
- **Plugin Manager** — загрузка, инициализация, управление плагинами
- **Конфигурация** — JSON-файл `plugins/plugins_config.json` для настройки
- **API endpoints** — управление плагинами через REST API

#### 📦 Плагин Google Calendar
- **Отключаемый модуль** — можно выключить через `/api/plugins`
- **Изоляция зависимостей** — ошибки плагина не влияют на ядро
- **Настройки** — интервал синхронизации, напоминания по умолчанию

#### 🧠 MCP Integration (Supabase)
- **Supabase SDK** — замена прямых HTTP-запросов на типизированный клиент
- **MCP инструменты** — новые функции для AI:
  - `search_my_memories` — поиск в памяти
  - `get_my_health_stats` — статистика здоровья
  - `list_my_active_reminders` — список напоминаний
- **Упрощение кода** — `memory.py` и `reminders.py` переписаны с использованием SDK

#### 📊 Smart Summary
- **Еженедельные отчёты** — команда `/summary [days]`
- **AI-аналитика** — анализ продуктивности, здоровья, памяти
- **Тренды** — сравнение с предыдущим периодом
- **Детализация** — по категориям (здоровье, календарь, воспоминания)

#### ⚙️ Настройки Аур
- **Команда `/aurasettings`** — управление аурами
- **Гибкая настройка** — включение/выключение, время
- **Сохранение в БД** — таблица `aura_settings`
- **7 типов аур**:
  - Emoji-статус
  - Био-статус
  - Напоминание о здоровье
  - Утреннее приветствие
  - Вечернее напоминание
  - Обед
  - Перерыв

---

### 🛠 Технические изменения

#### Обновлённые файлы
| Файл | Изменения |
|------|-----------|
| `requirements.txt` | Добавлен `supabase` SDK |
| `brains/clients.py` | Добавлен `supabase_client` |
| `brains/memory.py` | Полная переработка на Supabase SDK |
| `brains/reminders.py` | Переработка на Supabase SDK |
| `brains/ai.py` | Добавлены 3 новых MCP инструмента |
| `main.py` | Интеграция плагин-системы, API endpoints |

#### Новые файлы
| Файл | Описание |
|------|----------|
| `brains/mcp_tools.py` | MCP инструменты для работы с Supabase |
| `brains/smart_summary.py` | Генерация еженедельных отчётов |
| `brains/aura_settings.py` | Менеджер настроек аур |
| `plugins/base.py` | Базовый класс плагина и Plugin Manager |
| `plugins/__init__.py` | Экспорт плагин-системы |
| `plugins/google_calendar.py` | Плагин Google Calendar |
| `plugins/plugins_config.json` | Конфигурация плагинов |

#### Изменения в БД
```sql
-- Новая таблица: reminders
CREATE TABLE reminders (...);

-- Новая таблица: aura_settings
CREATE TABLE aura_settings (...);
```

---

### 📋 Новые команды бота

| Команда | Описание |
|---------|----------|
| `/summary [days]` | Еженедельный отчёт за указанный период |
| `/aurasettings` | Показать настройки аур |
| `/aurasettings enable <name> [time]` | Включить ауру |
| `/aurasettings disable <name>` | Выключить ауру |

---

### 🔌 API endpoints

| Endpoint | Метод | Описание |
|----------|-------|----------|
| `/api/plugins` | GET | Список всех плагинов |
| `/api/plugins/<name>/enable` | POST | Включить плагин |
| `/api/plugins/<name>/disable` | POST | Выключить плагин |
| `/api/plugins/<name>/settings` | GET/POST | Настройки плагина |

---

### 🚀 Миграция

#### 1. Обновление зависимостей
```bash
pip install -r requirements.txt
```

#### 2. Обновление БД
```bash
# Выполните новый init.sql в Supabase
psql -f docs/init.sql
```

#### 3. Проверка конфигурации
```bash
# Убедитесь, что plugins/plugins_config.json существует
# Отрегулируйте настройки плагинов при необходимости
```

---

### ⚠️ Breaking Changes

- **Память (memory.py)** — старый код с HTTP-запросами больше не работает
- **Напоминания (reminders.py)** — требуется таблица `reminders` в БД
- **Плагины** — при отключении плагина Google Calendar функции календаря будут недоступны

---

### 🐛 Исправления

- Улучшена обработка ошибок Supabase
- Добавлен Circuit Breaker для AI запросов
- Оптимизировано переиспользование HTTP соединений

---

### 📈 Метрики проекта (обновлено)

| Метрика | Значение |
|---------|----------|
| **Файлов** | ~30 |
| **Строк кода** | ~4500+ |
| **Модулей** | 15 (brains: 12, plugins: 3) |
| **Плагинов** | 1 (Google Calendar) |
| **Команд бота** | 11 |
| **API endpoints** | 10 |

---

## v2.0 — Январь 2026 (архив)

### Выполнено
- RAG память на Supabase (pgvector)
- Google Calendar интеграция
- Telegram Mini App (3D bubble UI)
- Эмоциональный движок
- Умные напоминания с эскалацией

---

## v1.0 — Декабрь 2025 (архив)

### MVP
- Mistral AI Integration
- Базовые ауры (emoji, bio)
- Голосовая транскрибация
- Утренние брифинги
