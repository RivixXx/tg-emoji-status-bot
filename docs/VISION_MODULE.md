# 👁️ Модуль компьютерного зрения Karina AI

## 📋 Обзор

Полноценная система **компьютерного зрения** на базе Mistral AI (Pixtral) для анализа изображений, распознавания текста и извлечения данных.

---

## ✨ Возможности

### 📝 OCR — Распознавание текста
- Чтение текста с фотографий
- Поддержка многоязычного распознавания
- Сохранение структуры (таблицы, списки)

### 📄 Анализ документов
- Паспорта, права, справки
- Автоматическое определение типа
- Извлечение полей (ФИО, дата, номер)

### 🧾 Анализ чеков
- Распознавание товаров и цен
- Извлечение даты, магазина, суммы
- Структурирование данных

### 🖼️ Анализ сцен
- Описание фотографий
- Распознавание объектов
- Определение места и настроения

### 📱 Анализ скриншотов
- Интерфейсы приложений
- Сообщения об ошибках
- Переписки

---

## 📱 Команды

### Автоматический анализ

**Просто отправь фото в чат** — Карина автоматически проанализирует его!

```
[Отправляешь фото]
↓
Карина: 🖼️ Анализ изображения:
[Описание что на фото + распознанный текст]
```

---

### `/vision` — Справка

Показывает возможности и команды модуля.

---

### `/ocr` — Распознавание текста

**Использование:**
```
1. Отправь фото с текстом
2. Ответь командой /ocr на это фото
```

**Пример:**
```
[Фото документа]
→ /ocr

Карина:
📝 Распознанный текст:

ООО "Рога и Копыта"
ИНН 1234567890
г. Москва, ул. Ленина 1
...
```

---

### `/analyze` — Детальный анализ

**Использование:**
```
1. Отправь фото
2. Ответь командой /analyze
```

**Пример:**
```
[Фото офиса]
→ /analyze

Карина:
🖼️ Анализ:

На фотографии изображён современный офис...
[детальное описание]
```

---

### `/doc` — Анализ документа

**Использование:**
```
1. Отправь фото документа
2. Ответь командой /doc
```

**Пример:**
```
[Фото паспорта]
→ /doc

Карина:
📄 Тип: Паспорт гражданина РФ

Данные:
ФИО: Иванов Иван Иванович
Дата рождения: 01.01.1990
Серия: 1234 Номер: 567890
...

Хочешь, я запомню важные данные? Напиши /remember
```

---

### `/receipt` — Анализ чека

**Использование:**
```
1. Отправь фото чека
2. Ответь командой /receipt
```

**Пример:**
```
[Фото чека из магазина]
→ /receipt

Карина:
🧾 Анализ чека:

Магазин: Пятёрочка
Дата: 22.02.2026 14:30
Сумма: 1 234.56₽

Товары:
• Хлеб бородинский — 45₽
• Молоко 3.2% — 89₽
• ...
```

---

### `/vision find <запрос>` — Поиск по фото

**Использование:**
```
/vision find паспорт
/vision find чек
/vision find ошибка
```

**Пример:**
```
/vision find справка

Карина:
🔍 Найдено 3 результатов:

1. 22.02.2026 14:30
   Файл: photo_2026-02-22.jpg
   Запрос: Проанализируй документ...
   Анализ: Медицинская справка №123...
```

---

## 🗄️ База данных

### Таблица `vision_history`

```sql
CREATE TABLE vision_history (
    id BIGSERIAL PRIMARY KEY,
    user_id BIGINT NOT NULL,
    image_hash TEXT NOT NULL,      -- MD5 хэш для поиска дублей
    original_filename TEXT,         -- Имя файла
    prompt TEXT,                    -- Запрос пользователя
    analysis TEXT NOT NULL,         -- Анализ от AI
    metadata JSONB DEFAULT {},      -- Доп. данные
    analyzed_at TIMESTAMPTZ DEFAULT NOW()
);
```

### Индексы

```sql
-- Для быстрого поиска по пользователю
CREATE INDEX idx_vision_history_user ON vision_history(user_id);

-- Для поиска дублей
CREATE INDEX idx_vision_history_hash ON vision_history(image_hash);

-- Полнотекстовый поиск по анализу
CREATE INDEX idx_vision_history_analysis 
  ON vision_history USING gin(to_tsvector('russian', analysis));
```

---

## 🔧 Как это работает

### 1. Загрузка фото

```python
# Пользователь отправляет фото
photo_path = await event.download_media(...)
```

### 2. Кодирование в base64

```python
def encode_image_to_base64(image_path):
    with open(image_path, "rb") as f:
        return base64.b64encode(f.read()).decode('utf-8')
```

### 3. Запрос к Mistral AI

```python
payload = {
    "model": "pixtral-12b-2409",  # Мультимодальная модель
    "messages": [{
        "role": "user",
        "content": [
            {"type": "image_url", "image_url": "data:image/jpeg;base64,{base64}"},
            {"type": "text", "text": "Опиши что на изображении"}
        ]
    }]
}

response = await http_client.post(MISTRAL_VISION_URL, json=payload)
```

### 4. Обработка ответа

```python
analysis_text = result['choices'][0]['message']['content']

# Сохраняем в БД
await save_vision_analysis(user_id, image_path, prompt, analysis_text)

# Возвращаем пользователю
await event.respond(f"🖼️ Анализ:\n\n{analysis_text}")
```

---

## 📊 Типы анализа

| Тип | Функция | Описание |
|-----|---------|----------|
| **Автоматический** | `analyze_image()` | Базовое описание + текст |
| **OCR** | `ocr_image()` | Только текст |
| **Документ** | `analyze_document()` | Тип + поля документа |
| **Чек** | `analyze_receipt()` | Товары + суммы |
| **Сцена** | `analyze_photo_scene()` | Описание обстановки |
| **Скриншот** | `analyze_screenshot()` | Интерфейс + действие |

---

## 🚀 Как активировать

### 1. Выполнить миграцию БД

```sql
-- В Supabase SQL Editor
-- Из файла docs/init_vision.sql
```

### 2. Проверить модель Mistral

Убедись что твой API ключ поддерживает `pixtral-12b-2409`:

```python
# brains/vision.py
VISION_MODEL = "pixtral-12b-2409"
```

### 3. Протестировать

```
/vision         → Справка
[Отправить фото] → Автоматический анализ
```

---

## ⚙️ Настройки

### Изменить модель

**Файл:** `brains/vision.py`

```python
# Было
VISION_MODEL = "pixtral-12b-2409"

# Стало (если доступна другая)
VISION_MODEL = "mistral-large-latest"  # Или другая мультимодальная
```

### Изменить лимит размера

```python
MAX_IMAGE_SIZE_MB = 10  # Максимум 10МБ
```

### Включить/отключить авто-анализ

**Файл:** `skills/__init__.py`

```python
# В chat_handler
if event.photo:
    # Закомментировать если не нужен авто-анализ
    # result = await analyze_image(...)
    # await event.respond(...)
```

---

## 📈 Метрики

### Полезные SQL запросы

```sql
-- Количество анализов за неделю
SELECT DATE(analyzed_at) as date, COUNT(*) 
FROM vision_history 
WHERE analyzed_at > NOW() - INTERVAL '7 days'
GROUP BY DATE(analyzed_at);

-- Популярные типы анализа (по ключевым словам)
SELECT 
    CASE 
        WHEN analysis LIKE '%документ%' THEN 'Документы'
        WHEN analysis LIKE '%чек%' OR analysis LIKE '%товар%' THEN 'Чеки'
        WHEN analysis LIKE '%текст%' THEN 'Текст'
        ELSE 'Другое'
    END as type,
    COUNT(*)
FROM vision_history
GROUP BY type;

-- Поиск дублей
SELECT image_hash, COUNT(*) 
FROM vision_history 
GROUP BY image_hash 
HAVING COUNT(*) > 1;
```

---

## 🔒 Безопасность

### Хранение изображений

- **Оригиналы** не хранятся (только хэш MD5)
- **Временные файлы** удаляются после анализа
- **Анализ** сохраняется в текстовом виде

### Конфиденциальность

```sql
-- Данные доступны только пользователю
SELECT * FROM vision_history WHERE user_id = {твой_id};
```

---

## 🎯 Примеры использования

### 1. Быстрое сохранение данных

```
[Фото паспорта]
→ /doc

Карина извлекает данные
→ /remember ФИО: Иванов Иван Иванович
```

### 2. Анализ расходов

```
[Фото чека из ресторана]
→ /receipt

Карина: Сумма: 5 678₽
→ Сохраняем в трекер расходов
```

### 3. Работа с документами

```
[Фото справки]
→ /doc

Карина извлекает:
- Номер справки
- Дата выдачи
- Срок действия

→ /remember Срок справки: 22.02.2027
```

### 4. Поиск информации

```
/vision find ИНН
→ Находит все документы с ИНН

/vision find ошибка
→ Находит скриншоты с ошибками
```

---

## 🛠️ Troubleshooting

### "API error: 400"

**Причина:** Модель не поддерживает изображения

**Решение:**
```python
# Проверь доступные модели Mistral
VISION_MODEL = "pixtral-12b-2409"  # Должна поддерживать vision
```

### "Файл слишком большой"

**Решение:**
```python
MAX_IMAGE_SIZE_MB = 20  # Увеличить лимит
```

### "Текст не распознан"

**Причина:** Низкое качество фото

**Решение:**
- Сделать фото при лучшем освещении
- Использовать более высокое разрешение

---

## 📚 Файлы модуля

| Файл | Описание |
|------|----------|
| `brains/vision.py` | Ядро модуля (анализ, OCR, поиск) |
| `docs/init_vision.sql` | Миграция БД |
| `skills/__init__.py` | Команды и обработка фото |
| `main.py` | Список команд бота |

---

**Последнее обновление:** Февраль 2026
