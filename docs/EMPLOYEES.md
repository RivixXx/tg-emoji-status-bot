# 👥 Сотрудники и Дни Рождения

## 📋 Обзор

Karina AI теперь хранит базу сотрудников компании с автоматическим отслеживанием дней рождений и генерацией поздравлений.

---

## 🗄️ Структура БД

### Таблица `employees`

```sql
CREATE TABLE employees (
    id BIGSERIAL PRIMARY KEY,
    full_name TEXT NOT NULL,           -- Полное ФИО
    first_name TEXT NOT NULL,          -- Имя
    last_name TEXT NOT NULL,           -- Фамилия
    position TEXT NOT NULL,            -- Должность
    department TEXT NOT NULL,          -- Отдел
    characteristics TEXT,              -- Описание качеств для поздравлений
    birthday DATE,                     -- Дата рождения (YYYY-MM-DD)
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW()
);
```

---

## 🚀 Как добавить сотрудников

### Способ 1: Через Supabase SQL Editor

1. Открой Supabase Dashboard → SQL Editor
2. Скопируй содержимое `docs/seed_employees.sql`
3. Выполни скрипт

### Способ 2: Через MCP инструмент

```python
# Пример добавления сотрудника через Python
from brains.mcp_tools import mcp_add_employee

await mcp_add_employee({
    "full_name": "Иванов Иван",
    "first_name": "Иван",
    "last_name": "Иванов",
    "position": "Менеджер",
    "department": "Отдел продаж",
    "characteristics": "Ответственный и коммуникабельный",
    "birthday": "1990-05-15"
})
```

### Способ 3: Через Telegram команду (в разработке)

```
/employeadd Иванов Иван|Менеджер|Отдел продаж|1990-05-15
```

---

## 📱 Команды бота

### `/employees` — Список всех сотрудников

Показывает структурированный список по отделам с датами рождений.

**Пример:**
```
👥 Сотрудники компании:

**Монтажный отдел:**
• Зуев Сергей — Монтажный специалист (08-14)
• Каширов Сергей — Монтажный специалист (08-09)

**Отдел продаж:**
• Василенко Вадим — Менеджер по продажам (05-06)
```

### `/birthdays [days]` — Ближайшие дни рождения

Показывает предстоящие дни рождения в указанный период (по умолчанию 7 дней).

**Примеры:**
```
/birthdays      → Показать на неделю
/birthdays 14   → Показать на 2 недели
/birthdays 30   → Показать на месяц
```

**Пример ответа:**
```
🎂 Ближайшие дни рождения (7 дн.):

• Ушаков Иван — 02-16 (через 3 дн.)
• Чиркин Алексей — 02-01 (через 5 дн.)
```

---

## 🤖 AI инструменты (Function Calling)

### `check_employee_birthdays`

Проверяет дни рождения сегодня.

**Использование AI:**
```
Пользователь: "У кого сегодня день рождения?"
Карина: [вызывает check_employee_birthdays]
Карина: "Сегодня день рождения празднуют: Ушаков Иван, Чиркин Алексей! 🥳"
```

### `get_upcoming_employee_birthdays`

Получает предстоящие дни рождения.

**Параметры:**
- `days` (int, default: 7) — период для поиска

**Пример:**
```
Пользователь: "Какие ближайшие дни рождения?"
Карина: [вызывает get_upcoming_employee_birthdays(days=7)]
```

---

## 🎂 Автоматические поздравления

Karina автоматически проверяет дни рождения в 9:05 утра (см. `auras/__init__.py`).

### Логика работы:

1. **Проверка** — каждый день в 9:05
2. **Генерация поздравления** — AI создаёт уникальное поздравление на основе характеристик сотрудника
3. **Отправка** — в личный чат Михаилу
4. **Генерация открытки** — (TODO) DALL-E 3 генерация

### Пример поздравления:

```
🥳 **С ДНЁМ РОЖДЕНИЯ!** 🎂

Сегодня день рождения у Ивана Ушакова!

Иван — компетентный и уважаемый коллегами 
профессионал. Делает работу качественно и без 
лишнего шума, отличается ответственностью и 
стабильностью.

Желаем профессиональных успехов и надёжных 
партнёров! 🎉
```

---

## 📊 MCP инструменты

| Инструмент | Описание |
|------------|----------|
| `mcp_get_todays_birthdays()` | Дни рождения сегодня |
| `mcp_get_upcoming_birthdays(days)` | Предстоящие ДР |
| `mcp_get_all_employees()` | Все сотрудники |
| `mcp_get_employee_by_id(id)` | Сотрудник по ID |
| `mcp_add_employee(data)` | Добавить сотрудника |
| `mcp_update_employee(id, data)` | Обновить данные |
| `mcp_delete_employee(id)` | Удалить сотрудника |
| `mcp_generate_birthday_card(emp)` | Промт для открытки |

---

## 🔧 Интеграция с другими модулями

### Календарь

Дни рождения можно автоматически добавлять в Google Calendar:

```python
# В разработке
await add_calendar(
    summary=f"🎂 День рождения: {employee['full_name']}",
    start_time=birthday_datetime,
    recurring=True  # Ежегодно
)
```

### Напоминания

Автоматическое напоминание за 1 день до ДР:

```python
# В разработке
reminder = Reminder(
    id=f"birthday_{employee_id}_{year}",
    type=ReminderType.BIRTHDAY,
    message=f"Завтра день рождения у {employee['full_name']}!",
    scheduled_time=reminder_time
)
```

---

## 📝 Структура отделов

На февраль 2026 в базе 30 сотрудников:

| Отдел | Количество |
|-------|------------|
| Руководство | 5 |
| Финансовый блок | 4 |
| Административный блок | 1 |
| Отдел продаж | 5 |
| Проектное направление | 1 |
| Технический отдел | 3 |
| Монтажный отдел | 8 |
| Эксплуатация и склад | 2 |
| Поддержка | 1 |

---

## 🛠️ Troubleshooting

### "Сотрудники не отображаются"

**Решение:**
```sql
-- Проверить наличие данных
SELECT COUNT(*) FROM employees;

-- Если 0, выполнить seed_employees.sql
```

### "Дни рождения не находятся"

**Причина:** Неправильный формат даты

**Решение:**
```sql
-- Проверить формат
SELECT full_name, birthday FROM employees;

-- Должно быть: 1970-02-16 (YYYY-MM-DD)
-- Исправить если нужно:
UPDATE employees 
SET birthday = '1970-02-16' 
WHERE full_name = 'Ушаков Иван';
```

### "AI не вызывает инструменты"

**Причина:** Не добавлены инструменты в `brains/ai.py`

**Решение:** Проверить что в `TOOLS` есть:
- `check_employee_birthdays`
- `get_upcoming_employee_birthdays`

---

## 📚 Файлы

| Файл | Описание |
|------|----------|
| `docs/init.sql` | Схема таблицы employees |
| `docs/seed_employees.sql` | Данные для заполнения |
| `docs/init_employees.sql` | Полная инициализация |
| `brains/employees.py` | Модуль работы с сотрудниками |
| `brains/mcp_tools.py` | MCP инструменты |
| `brains/ai.py` | AI Function Calling |
| `skills/__init__.py` | Telegram команды |

---

**Последнее обновление:** Февраль 2026
