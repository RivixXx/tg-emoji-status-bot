# 🔧 Исправление дублирования сообщений

## Проблема

При отправке команды `/news` бот присылал ответ **несколько раз** + добавлял лишнюю информацию (календарь, память из AI).

---

## Причина

1. **Дублирование хендлеров** — команда `/news` обрабатывалась в двух местах:
   - Старый хендлер (строка 143)
   - Новый хендлер (строка 350)

2. **Отсутствие StopPropagation** — после выполнения команды обработка продолжалась в `chat_handler`, который вызывал AI.

---

## Решение

### 1. Удалён старый хендлер `/news`

**Было:**
```python
@client.on(events.NewMessage(pattern='/news'))
async def news_handler(event):
    logger.info(f"📩 /news от пользователя {event.chat_id}")
    news = await get_latest_news()
    await event.respond(f"🗞 **Новости:**\n\n{news}")
```

**Стало:** Удалено (оставлен только новый расширенный хендлер)

---

### 2. Добавлен `StopPropagation` во все команды

Теперь каждая команда явно останавливает дальнейшую обработку:

```python
@client.on(events.NewMessage(pattern='/calendar'))
async def calendar_handler(event):
    info = await get_upcoming_events()
    await event.respond(f"🗓 **Твои планы:**\n\n{info}")
    raise events.StopPropagation  # ← Важно!
```

**Обновлённые команды:**
- `/start`
- `/app`
- `/calendar`
- `/conflicts`
- `/health`
- `/remember`
- `/weather`
- `/clearrc`
- `/summary`
- `/aurasettings`
- `/employees`
- `/birthdays`
- `/news`
- `/newsforce`
- `/newssources`
- `/newsclear`

---

## Результат

✅ Команды выполняются **один раз**  
✅ AI не добавляет лишнюю информацию  
✅ Календарь и память показываются только по соответствующим командам

---

## Проверка

```bash
# Протестировать команды
/news              → Должен прийти один ответ с новостями
/news force        → Один ответ с обновлением
/calendar          → Один ответ с планами
```

---

## Файлы

- `skills/__init__.py` — обновлены все хендлеры команд

---

**Дата исправления:** 22 февраля 2026
